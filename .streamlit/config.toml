[theme]
base = "light"
primaryColor = "#ea580c"
backgroundColor = "#eff6ff"
secondaryBackgroundColor = "#ffffff"
textColor = "#0f172a"
font = "sans serif"

[client]
toolbarMode = "minimal"

# medicalsearch vb. ile çakışmaması için (varsayılan 8501 yerine)
[server]
port = 9517
address = "localhost"
